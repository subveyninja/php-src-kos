set(NGINX_VERSION 1.0.0)

@PACKAGE_INIT@

add_executable(Fpm IMPORTED)
set_property(TARGET Fpm APPEND PROPERTY IMPORTED_CONFIGURATIONS RELEASE)
set_target_properties(Fpm
	PROPERTIES
		IMPORTED_LOCATION_RELEASE "@PACKAGE_BIN_INSTALL_DIR@/Fpm"
)

check_required_components(php)
